<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="">
        <meta name="author" content="">

        <title>Hello Phoenix!</title>
        <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
        <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
        <script src="//code.jquery.com/jquery-1.11.3.min.js"></script>
    </head>

    <body>
        <div class="container" role="main">
            <div class="header">
                <%= if @current_user do %>
                    You are logged in!
                <% else %>
                    You are not logged in.
                <% end %>
                <ul class="nav nav-pills pull-right">
                    <li><a href="http://www.phoenixframework.org/docs">Get Started</a></li>
                </ul>
                <span class="logo"></span>
                <nav>
                    <ul>
                        <li><a href="<%= page_path @conn, :index %>">Home</a></li>
                        <li><a href="<%= login_path @conn, :login %>">Guest Login</a></li>
                        <li><a href="<%= user_path @conn, :index %>">Users</a></li>
                        <li><a href="<%= admin_path @conn, :index %>">Admin</a></li>
                        <%= if @current_user do %>
                            <li><a href="<%= game_path @conn, :index %>">Play!</a></li>
                        <%= else %>
                        <%= end %>
                        <li><a href="<%= game_path @conn, :test %>">Test</a></li>
                    </ul>
                </nav>
                <ol class="breadcrumb text-right">
                    <%= if @current_user do %>
                        <%= if @conn.assigns[:remembered] do %>
                            <li style="color: red;">!!</li>
                        <% end %>
                    <% end %>
                </ol>
            </div>

            <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
            <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

            <%= render @view_module, @view_template, assigns %>
        </div>
    </body>
</html>
